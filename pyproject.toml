[build-system]
requires = ["setuptools>=42"]
build-backend = "setuptools.build_meta"


[project]
name = "ballbox"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["numpy", "matplotlib"]

[project.scripts]
ballbox = "ballbox.main:main"
